package dek.algorithm.aray_and_string;

public class ArrayAndString {
    /**
     * Check if a string has all unique characters. You 're not allowed to use additional data structures
     * @return
     */
    public boolean isUnique() {
        return true;
    }

    /**
     * Check if two strings are permutation of each others (differs from finding permutation in string s)
     * @param a
     * @param b
     */
    public boolean checkPermutation(String a, String b) {
        return true;
    }

    /**
     * Write method to replace all spaces in a string with '%20'
     * @param target
     * @return
     */
    public String URLify(String target) {
        return null;
    }

    /**
     * Given a string, write a function to check if it is a permutation of a palindrome.
     * A palindrome is a word or phrase that is the same forwards and backwards.
     * A permutation is a rearrangement of letters
     * @param target
     * @return
     */
    public boolean palindromePermutation(String target) {
        return true;
    }

    /**
     * There are  three  types of  edits  that can be  performed  on  strings:  insert  a character,
     * remove a character, or replace a character. Given two strings, write a  function to check if they are
     * one edit (or zero edits) away
     * @return
     */
    public boolean oneAway(String a, String b) {
        return true;
    }

    /**
     * Assumeyou have a method isSubstringwhich checks  if oneword  is a substring
     * of another. Given two strings, sl and s2, write code to check if s2 is a rotation of sl using only one
     * call to isSubstring.
     * @param s1
     * @param s2
     * @return
     */
    public boolean isRotation(String s1, String s2) {
        return true;
    }

    public int[] threeLargest(int[] nums) {
        return null;
    }
}
